# Deep Dish recovery — 2026-08-28

## Why this rollback was made

The production-facing `main` branch had moved from the Restaurant Intelligence Hub / corpus application to a Grok confirmation-pass rebuild. That rebuild intentionally left the full corpus on the `corpus` branch, but it also removed the national dataset, corpus pipeline, richer case-file interface, and editorial hero assets from `main`.

## Restored source

`main` was restored to corpus commit `0cae78b69cae77432d47a16b242d695152e090c9` before this documentation commit was added.

That corpus snapshot reports:

- 836 restaurant records
- 836 enriched records
- 824 resolved records
- all 51 state/DC coverage units represented
- 95% average completeness
- zero thin records at the recorded coverage pass

It also restores the Restaurant Intelligence Hub UI, situation-aware filtering, case files, compare tools, scenario playbooks, the run-planner/corpus pipeline, and the `hero-pass.jpg` / `fig-gold.jpg` visual assets.

## Safety copy

The displaced Grok rebuild was preserved before rollback at:

`backup/grok-rebuild-2026-08-28`

Do not delete that branch until the restored production application has been visually and functionally verified.

## Guardrail

Do not replace the corpus application wholesale again. New instruments should be integrated into the existing Restaurant Intelligence Hub, or developed on a separate branch/application surface, without deleting the canonical dataset, data pipeline, imagery, or case-file UX.
