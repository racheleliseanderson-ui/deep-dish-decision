# Pipeline secrets (never commit real values)
# LOVABLE_API_KEY=          # only if using Lovable gateway for Google
# GOOGLE_MAPS_API_KEY=      # Places server key (gateway or direct later)
FIRECRAWL_API_KEY=fc-your-key-here
# FIRECRAWL_DIRECT=1        # default: call api.firecrawl.dev directly
